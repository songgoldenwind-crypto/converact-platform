services:
  converact:
    build: .
    ports:
      - "${CONVERACT_HOST_PORT:-3000}:3000"
    environment:
      PORT: "3000"
      CONVERACT_DB_PATH: "/data/opc.sqlite"
    volumes:
      - converact_data:/data

volumes:
  converact_data:
