{
  "schema_version": 1,
  "checkpoint": "RM01-R1-tenant-jwks-scheduler",
  "generated_at": "2026-08-24T03:38:40Z",
  "cycles": [
    {
      "id": "bounded_schedule_and_status_contract",
      "red": "the focused test could not import scheduler configuration, status or exit types because no periodic runtime boundary existed",
      "green": "one scheduler validates period, jitter and seed against the exact lifecycle freshness policy and exposes a one-value watch status"
    },
    {
      "id": "deterministic_jitter",
      "red": "there was no stable per-attempt delay or bound preventing a scheduled refresh from landing at or beyond key expiry",
      "green": "10,000 deterministic samples remain within period plus or minus jitter and maximum delay is strictly below freshness expiry"
    },
    {
      "id": "failure_without_retry_burst",
      "red": "the scheduler contract lacked a failed-attempt observation and proof that recovery waits the complete next slot",
      "green": "failure is published as a closed value-free status and the next full deterministic slot can complete successfully"
    },
    {
      "id": "cooperative_shutdown",
      "red": "there was no periodic future that could stop before a sleeping or active refresh under the process shutdown signal",
      "green": "sleep shutdown starts no fetch and active-fetch shutdown drops the cancellation-safe future and releases its exact lease"
    },
    {
      "id": "single_flight_observation",
      "red": "the scheduler InFlight result branch had no direct proof that an existing request-time owner prevented a second fetch",
      "green": "the scheduler publishes InFlight, performs no additional fetch and leaves the existing owner intact"
    },
    {
      "id": "source_boundary_false_positive",
      "red": "the source-boundary test matched the documentation phrase unsafe period even though no unsafe code existed",
      "green": "the ambiguous prose was removed and the unchanged source prohibition now passes without weakening the check"
    }
  ]
}
