[package]
name = "converact-postgres-store"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
converact-audit = { path = "../audit" }
converact-agent-handoff-core = { path = "../agent-handoff-core" }
converact-agent-handoff-store = { path = "../agent-handoff-store" }
converact-contracts = { path = "../contracts" }
converact-conversation-result-core = { path = "../conversation-result-core" }
converact-conversation-result-store = { path = "../conversation-result-store" }
converact-event-log = { path = "../event-log" }
converact-idempotency = { path = "../idempotency" }
converact-kernel-ids = { path = "../kernel-ids" }
converact-migration-routing = { path = "../migration-routing" }
converact-migration-store = { path = "../migration-store" }
converact-outbox-worker = { path = "../outbox-worker" }
converact-tool-broker-core = { path = "../tool-broker-core" }
converact-tool-broker-store = { path = "../tool-broker-store" }
converact-voice-agent-contracts = { path = "../voice-agent-contracts" }
deadpool-postgres.workspace = true
hex.workspace = true
serde_json.workspace = true
sha2.workspace = true
tokio.workspace = true
tokio-postgres = { workspace = true, features = ["with-serde_json-1"] }

[lints]
workspace = true
