#!KAMAILIO
# This file is intentionally not a runnable SIP edge configuration.
#
# Production configuration contains topology-hiding and loopback RPC secrets and
# must be rendered at deployment time with:
#
#   npm run converact:kamailio:render
#
# Use kamailio.runtime.example.json for the non-secret schema. The renderer reads
# secrets from files, writes kamailio.cfg and tls.cfg atomically with mode 0600,
# and refuses inline secret environment variables.

#!error "render Kamailio configuration with npm run converact:kamailio:render"
