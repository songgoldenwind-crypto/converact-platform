apiVersion: batch/v1
kind: Job
metadata:
  name: ivekit-capacity-scaling-finalizer
  labels:
    app.kubernetes.io/name: ivekit-capacity-scaling-finalizer
spec:
  backoffLimit: 10
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ivekit-capacity-scaling-finalizer
    spec:
      restartPolicy: OnFailure
      terminationGracePeriodSeconds: 30
      containers:
        - name: scaling-finalizer
          image: replace.example.com/ivekit/capacity-tools@sha256:replace-with-capacity-tools-digest
          imagePullPolicy: IfNotPresent
          command: ["node", "--import", "tsx", "scripts/ivekit-capacity-scaling-finalizer.ts"]
          env:
            - name: CONVERACT_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ivekit-capacity-runtime
                  key: database-url
            - name: CONVERACT_FABRIC_CAPACITY_SCALING_FINALIZER_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: CONVERACT_FABRIC_CAPACITY_SCALING_CONTRACT_PATH
              value: /run/ivekit-capacity/scaling-contract.json
            - name: CONVERACT_FABRIC_CAPACITY_SCALING_SUBMISSION_PATH
              value: /run/ivekit-capacity/scaling-submission.json
            - name: CONVERACT_FABRIC_CAPACITY_SCALING_FINALIZER_LEASE_MS
              value: "15000"
            - name: CONVERACT_FABRIC_CAPACITY_SCALING_EVIDENCE_PREFIX
              valueFrom:
                configMapKeyRef:
                  name: ivekit-capacity-scaling-finalizer
                  key: scaling-evidence-prefix
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: ivekit-capacity-finalizer
                  key: evidence-s3-bucket
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_REGION
              valueFrom:
                configMapKeyRef:
                  name: ivekit-capacity-finalizer
                  key: evidence-s3-region
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: ivekit-capacity-finalizer
                  key: evidence-s3-endpoint
                  optional: true
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_FORCE_PATH_STYLE
              valueFrom:
                configMapKeyRef:
                  name: ivekit-capacity-finalizer
                  key: evidence-s3-force-path-style
                  optional: true
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: ivekit-capacity-evidence
                  key: evidence-s3-access-key-id
                  optional: true
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: ivekit-capacity-evidence
                  key: evidence-s3-secret-access-key
                  optional: true
          volumeMounts:
            - name: campaign-inputs
              mountPath: /run/ivekit-capacity
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: campaign-inputs
          persistentVolumeClaim:
            claimName: ivekit-capacity-manifests
