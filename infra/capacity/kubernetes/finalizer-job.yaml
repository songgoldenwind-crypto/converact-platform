apiVersion: batch/v1
kind: Job
metadata:
  name: converact-capacity-finalizer
  labels:
    app.kubernetes.io/name: converact-capacity-finalizer
spec:
  backoffLimit: 10
  template:
    metadata:
      labels:
        app.kubernetes.io/name: converact-capacity-finalizer
    spec:
      restartPolicy: OnFailure
      terminationGracePeriodSeconds: 30
      containers:
        - name: finalizer
          image: replace.example.com/converact/capacity-tools@sha256:replace-with-capacity-tools-digest
          imagePullPolicy: IfNotPresent
          command: ["node", "--import", "tsx", "scripts/converact-capacity-finalizer.ts"]
          env:
            - name: CONVERACT_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: converact-capacity-runtime
                  key: database-url
            - name: CONVERACT_FABRIC_CAPACITY_FINALIZER_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: CONVERACT_FABRIC_CAPACITY_MANIFEST_PATH
              value: /run/converact-capacity/manifest.json
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_SUBMISSION_PATH
              value: /run/converact-capacity/evidence-submission.json
            - name: CONVERACT_FABRIC_CAPACITY_FINALIZER_LEASE_MS
              value: "15000"
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_PREFIX
              valueFrom:
                configMapKeyRef:
                  name: converact-capacity-finalizer
                  key: evidence-prefix
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_BUCKET
              valueFrom:
                configMapKeyRef:
                  name: converact-capacity-finalizer
                  key: evidence-s3-bucket
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_REGION
              valueFrom:
                configMapKeyRef:
                  name: converact-capacity-finalizer
                  key: evidence-s3-region
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: converact-capacity-finalizer
                  key: evidence-s3-endpoint
                  optional: true
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_FORCE_PATH_STYLE
              valueFrom:
                configMapKeyRef:
                  name: converact-capacity-finalizer
                  key: evidence-s3-force-path-style
                  optional: true
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: converact-capacity-evidence
                  key: evidence-s3-access-key-id
                  optional: true
            - name: CONVERACT_FABRIC_CAPACITY_EVIDENCE_S3_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: converact-capacity-evidence
                  key: evidence-s3-secret-access-key
                  optional: true
          volumeMounts:
            - name: evidence
              mountPath: /run/converact-capacity
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: evidence
          persistentVolumeClaim:
            claimName: converact-capacity-manifests
