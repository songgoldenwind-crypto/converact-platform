apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ivekit-otel-collector
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ivekit-otel-collector
      app.kubernetes.io/component: trace-gateway
