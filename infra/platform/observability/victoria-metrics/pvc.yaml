apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ivekit-victoria-metrics-data
  labels:
    app.kubernetes.io/name: ivekit-victoria-metrics
    app.kubernetes.io/component: metrics-storage
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 200Gi
