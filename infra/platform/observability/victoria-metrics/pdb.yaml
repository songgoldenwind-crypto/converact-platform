apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ivekit-victoria-metrics
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ivekit-victoria-metrics
      app.kubernetes.io/component: metrics-storage
