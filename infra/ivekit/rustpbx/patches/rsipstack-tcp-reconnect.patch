diff --git a/src/transaction/transaction.rs b/src/transaction/transaction.rs
index fca8e6a..aa6db95 100644
--- a/src/transaction/transaction.rs
+++ b/src/transaction/transaction.rs
@@ -303,7 +303,37 @@ impl Transaction {
             self.original.to_owned().into()
         };

-        connection.send(message, self.destination.as_ref()).await?;
+        if let Err(error) = connection
+            .send(message.clone(), self.destination.as_ref())
+            .await
+        {
+            if !is_retryable_tcp_send_error(&connection, &error) {
+                return Err(error);
+            }
+
+            debug!(
+                key = %self.key,
+                error = %error,
+                "TCP send failed; reconnecting once"
+            );
+            self.endpoint_inner
+                .transport_layer
+                .del_connection_if_same(&connection);
+            let destination = self.destination.clone().ok_or(Error::TransactionError(
+                "no destination found for TCP reconnect".to_string(),
+                self.key.clone(),
+            ))?;
+            let (retry_connection, resolved_addr) = self
+                .endpoint_inner
+                .transport_layer
+                .lookup(&destination, Some(&self.key))
+                .await?;
+            self.destination.replace(resolved_addr);
+            retry_connection
+                .send(message, self.destination.as_ref())
+                .await?;
+            self.connection.replace(retry_connection);
+        }
         self.transition(TransactionState::Calling).map(|_| ())
     }

@@ -988,6 +1018,25 @@ impl Transaction {
     }
 }

+fn is_retryable_tcp_send_error(connection: &SipConnection, error: &Error) -> bool {
+    if !matches!(connection, SipConnection::Tcp(_)) {
+        return false;
+    }
+
+    matches!(
+        error,
+        Error::IoError(io_error)
+            if matches!(
+                io_error.kind(),
+                std::io::ErrorKind::BrokenPipe
+                    | std::io::ErrorKind::ConnectionAborted
+                    | std::io::ErrorKind::ConnectionReset
+                    | std::io::ErrorKind::NotConnected
+                    | std::io::ErrorKind::UnexpectedEof
+            )
+    )
+}
+
 impl Drop for Transaction {
     fn drop(&mut self) {
         self.cleanup();
diff --git a/src/transport/connection.rs b/src/transport/connection.rs
index cb49897..58839ed 100644
--- a/src/transport/connection.rs
+++ b/src/transport/connection.rs
@@ -227,6 +227,13 @@ impl SipConnection {
         }
     }

+    pub(crate) fn same_instance(&self, other: &Self) -> bool {
+        match (self, other) {
+            (SipConnection::Tcp(left), SipConnection::Tcp(right)) => left.same_instance(right),
+            _ => false,
+        }
+    }
+
     pub async fn send(&self, msg: SipMessage, destination: Option<&SipAddr>) -> Result<()> {
         match self {
             SipConnection::Channel(transport) => transport.send(msg).await,
diff --git a/src/transport/tcp.rs b/src/transport/tcp.rs
index e74dbb6..7b66332 100644
--- a/src/transport/tcp.rs
+++ b/src/transport/tcp.rs
@@ -91,6 +91,10 @@ impl TcpConnection {
     pub fn cancel_token(&self) -> Option<CancellationToken> {
         self.cancel_token.clone()
     }
+
+    pub(crate) fn same_instance(&self, other: &Self) -> bool {
+        Arc::ptr_eq(&self.inner, &other.inner)
+    }
 }

 #[async_trait::async_trait]
diff --git a/src/transport/transport_layer.rs b/src/transport/transport_layer.rs
index 406febe..c0c5b5b 100644
--- a/src/transport/transport_layer.rs
+++ b/src/transport/transport_layer.rs
@@ -158,6 +158,10 @@ impl TransportLayer {
         self.inner.del_connection(addr)
     }

+    pub(crate) fn del_connection_if_same(&self, connection: &SipConnection) {
+        self.inner.del_connection_if_same(connection)
+    }
+
     pub async fn lookup(
         &self,
         target: &SipAddr,
@@ -248,7 +252,7 @@ impl TransportLayerInner {
         self.listens.write().retain(|t| t.get_addr() != addr);
     }

-    pub(super) fn add_connection(&self, connection: SipConnection) {
+    pub(super) fn add_connection(self: &Arc<Self>, connection: SipConnection) {
         if let Some(remote_addr) = connection.get_remote_addr().cloned() {
             let mut connections = self.connections.write();
             connections.insert(remote_addr, connection.clone());
@@ -263,8 +267,21 @@ impl TransportLayerInner {
         self.connections.write().remove(addr);
     }

+    fn del_connection_if_same(&self, connection: &SipConnection) {
+        let Some(remote_addr) = connection.get_remote_addr() else {
+            return;
+        };
+        let mut connections = self.connections.write();
+        let should_remove = connections
+            .get(remote_addr)
+            .is_some_and(|current| current.same_instance(connection));
+        if should_remove {
+            connections.remove(remote_addr);
+        }
+    }
+
     async fn lookup(
-        &self,
+        self: &Arc<Self>,
         destination: &SipAddr,
         outbound: Option<&SipAddr>,
         key: Option<&TransactionKey>,
@@ -383,7 +400,7 @@ impl TransportLayerInner {
         }
     }

-    pub fn serve_connection(&self, transport: SipConnection) {
+    pub fn serve_connection(self: &Arc<Self>, transport: SipConnection) {
         let sub_token = self.cancel_token.child_token();
         let sender_clone = self.transport_tx.clone();
         let remote_addr = transport
@@ -391,6 +408,7 @@ impl TransportLayerInner {
             .map(|addr| addr.to_string())
             .unwrap_or_else(|| "-".to_string());
         info!(addr=%transport.get_addr(), remote=%remote_addr, "serve_connection: starting serve_loop");
+        let transport_layer = self.clone();
         tokio::spawn(async move {
             match sender_clone.send(TransportEvent::New(transport.clone())) {
                 Ok(()) => {
@@ -413,6 +431,7 @@ impl TransportLayerInner {
             }
             info!(addr=%transport.get_addr(), remote=%remote_addr, "transport serve_loop exited");
             transport.close().await.ok();
+            transport_layer.del_connection_if_same(&transport);
             sender_clone.send(TransportEvent::Closed(transport)).ok();
         });
     }
@@ -553,4 +572,45 @@ mod tests {

         Ok(())
     }
+
+    #[tokio::test]
+    async fn closed_tcp_connection_is_removed_before_reconnect() -> Result<()> {
+        use tokio::net::TcpListener;
+        use tokio::sync::oneshot;
+        use tokio::time::{sleep, timeout, Duration};
+
+        let listener = TcpListener::bind("127.0.0.1:0").await?;
+        let socket_addr = listener.local_addr()?;
+        let target = SipAddr {
+            r#type: Some(Transport::Tcp),
+            addr: socket_addr.into(),
+        };
+        let (first_closed_tx, first_closed_rx) = oneshot::channel();
+        let server = tokio::spawn(async move {
+            let (first, _) = listener.accept().await.unwrap();
+            drop(first);
+            first_closed_tx.send(()).unwrap();
+            let (_second, _) = listener.accept().await.unwrap();
+        });
+
+        let tl = super::TransportLayer::new(tokio_util::sync::CancellationToken::new());
+        let (first, _) = tl.lookup(&target, None).await?;
+        first_closed_rx.await.unwrap();
+
+        timeout(Duration::from_secs(2), async {
+            loop {
+                if !tl.inner.connections.read().contains_key(&target) {
+                    break;
+                }
+                sleep(Duration::from_millis(10)).await;
+            }
+        })
+        .await
+        .expect("closed TCP connection should leave the cache");
+
+        let (second, _) = tl.lookup(&target, None).await?;
+        assert!(!first.same_instance(&second));
+        server.abort();
+        Ok(())
+    }
 }
