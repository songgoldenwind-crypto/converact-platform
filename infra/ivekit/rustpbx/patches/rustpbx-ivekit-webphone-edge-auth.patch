diff --git a/src/auth/jwt_auth_backend.rs b/src/auth/jwt_auth_backend.rs
index 98d4b2e..8ed1741 100644
--- a/src/auth/jwt_auth_backend.rs
+++ b/src/auth/jwt_auth_backend.rs
@@ -1,6 +1,7 @@
 use anyhow::Result;
 use async_trait::async_trait;
 use rsipstack::sip::Header;
+use rsipstack::sip::prelude::HeadersExt;
 use tracing::info;
 
 use crate::call::{TransactionCookie, user::SipUser};
@@ -66,6 +67,16 @@ impl AuthBackend for JwtAuthBackend {
             }
         };
 
+        let from_user = original
+            .from_header()
+            .ok()
+            .and_then(|header| header.uri().ok())
+            .and_then(|uri| uri.user().map(str::to_owned));
+        if from_user.as_deref() != Some(user_id.as_str()) {
+            info!("JWT subject does not match SIP From identity");
+            return Ok(None);
+        }
+
         let realm = original.uri().host().to_string();
 
         if let Some(ref ub) = self.user_backend {
diff --git a/src/proxy/auth.rs b/src/proxy/auth.rs
index 2e36044..eb649e8 100644
--- a/src/proxy/auth.rs
+++ b/src/proxy/auth.rs
@@ -389,7 +389,9 @@ impl ProxyModule for AuthModule {
         // requests are handled earlier by the dialog_auth_cache branch.
         if let Some(ref registry) = self.server.pre_auth_registry {
             if let Some(source_addr) = self.get_source_addr(tx) {
-                if let Some(agent_id) = registry.lookup(&source_addr).await {
+                if let Some(agent_id) = registry.lookup(&source_addr).await
+                    && agent_id == tx_user.username
+                {
                     let realm = tx.original.uri().host().to_string();
                     let resolved = match self
                         .server
diff --git a/src/proxy/registrar.rs b/src/proxy/registrar.rs
index bd6549e..1c9b8da 100644
--- a/src/proxy/registrar.rs
+++ b/src/proxy/registrar.rs
@@ -421,7 +421,7 @@ impl ProxyModule for RegistrarModule {
         &self,
         _token: CancellationToken,
         tx: &mut Transaction,
-        _cookie: TransactionCookie,
+        cookie: TransactionCookie,
     ) -> Result<ProxyAction> {
         let method = tx.original.method();
         if !matches!(method, rsipstack::sip::Method::Register) {
@@ -438,14 +438,17 @@ impl ProxyModule for RegistrarModule {
             .unwrap_or_else(|| "unknown".to_string());
         metrics::sip::registration_received(&realm);
 
-        let user = match SipUser::try_from(&*tx) {
-            Ok(u) => u,
-            Err(e) => {
-                info!("failed to parse user: {:?}", e);
-                metrics::sip::registration_failed(&realm, "invalid_user");
-                tx.reply(rsipstack::sip::StatusCode::BadRequest).await.ok();
-                return Ok(ProxyAction::Abort);
-            }
+        let user = match cookie.get_user() {
+            Some(user) => user,
+            None => match SipUser::try_from(&*tx) {
+                Ok(user) => user,
+                Err(e) => {
+                    info!("failed to parse user: {:?}", e);
+                    metrics::sip::registration_failed(&realm, "invalid_user");
+                    tx.reply(rsipstack::sip::StatusCode::BadRequest).await.ok();
+                    return Ok(ProxyAction::Abort);
+                }
+            },
         };
 
         let registered_aor = match tx.original.to_header() {
@@ -463,6 +466,12 @@ impl ProxyModule for RegistrarModule {
                 return Ok(ProxyAction::Abort);
             }
         };
+        if registered_aor.user() != Some(user.username.as_str()) {
+            info!("Authenticated user does not match REGISTER To identity");
+            metrics::sip::registration_failed(&realm, "identity_mismatch");
+            tx.reply(rsipstack::sip::StatusCode::Forbidden).await.ok();
+            return Ok(ProxyAction::Abort);
+        }
 
         let default_expires = self.config.registrar_expires.unwrap_or(30);
         let global_expires = tx
